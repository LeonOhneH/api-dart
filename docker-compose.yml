services:
  api:
    build: .
    ports:
      - "8081:8081"
    volumes:
      # Persist Isar database and font cache across container restarts
      - api_data:/data
    environment:
      PORT: "8081"
      API_KEY: ""
      RATE_LIMIT: "30"
    restart: unless-stopped

volumes:
  api_data:
