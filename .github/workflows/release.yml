name: Release

on:
  push:
    tags:
      - "v*"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Log in to registry
        run: echo "${{ secrets.REGISTRY_PASSWORD }}" | docker login -u "${{ secrets.REGISTRY_USER }}" --password-stdin "${{ secrets.REGISTRY }}"

      - name: Pull cache image
        run: docker pull "${{ secrets.REGISTRY }}/dfb-data-bridge:latest" || true

      - name: Build image
        run: |
          docker build \
            --cache-from "${{ secrets.REGISTRY }}/dfb-data-bridge:latest" \
            -t "${{ secrets.REGISTRY }}/dfb-data-bridge:${{ github.ref_name }}" \
            -t "${{ secrets.REGISTRY }}/dfb-data-bridge:latest" \
            -t "${{ secrets.REGISTRY }}/dfb-data-bridge:${{ github.sha }}" \
            .

      - name: Push image
        run: docker push "${{ secrets.REGISTRY }}/dfb-data-bridge" --all-tags
